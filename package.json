{
  "name": "claude-memory-mcp",
  "version": "0.2.0",
  "description": "MCP Server zur Bekämpfung von Context Rot in Claude Code",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "claude-memory-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8",
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
